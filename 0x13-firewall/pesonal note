ls /etc/ufw

ll /etc/ufw

sudo vi /etc/ufw/before.rules

#add before filter rule check read me.

ls /etc | grep sysctl.

sudo vi /etc/sysctl.conf

# enter and uncomment 1pv4 to allow forwarding

# to reload file	
sudo sysctl -p